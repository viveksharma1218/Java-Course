package JavaCourse;

import JavaCourse.ChapterOne.Variable;
import JavaCourse.ChapterOne.Number;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    static void main() {
        // we will use this pattern to learn every chapter
        // First the Chapter name
        // Second we always use run method to show result
        // so if we don't want to see these results we can comment out these lines

        //Chapter One
        Variable.run();
        Number.run();
    }
}
